---
swagger: "2.0"
info:
  x-ibm-name: "categories"
  title: "Categories"
  version: "1.0.0-SNAPSHOT"
schemes:
- "https"
host: "$(catalog.host)"
basePath: "/categories/r1/v1"
consumes:
- "application/json"
produces:
- "application/json"
securityDefinitions:
  x-ibm-client-secret:
    type: "apiKey"
    description: ""
    in: "header"
    name: "X-IBM-Client-Secret"
  clientIdHeader:
    type: "apiKey"
    in: "header"
    name: "X-IBM-Client-Id"
security:
- clientIdHeader: []
  x-ibm-client-secret: []
x-ibm-configuration:
  testable: true
  enforced: true
  cors:
    enabled: true
  assembly:
    execute:
    - invoke:
        target-url: "https://cenco-category-service.mybluemix.net/category-api/category"
    catch: []
  phase: "realized"
paths:
  /categories:
    get:
      responses:
        200:
          description: "200 OK"
definitions: {}
tags: []